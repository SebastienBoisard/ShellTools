# ShellTools
Some of my shell tools

## python_notebook

[python_notebook.sh](python_notebook.sh) activates the right Anaconda Python environment (Python 2 or Python 3) and
starts the Jupyter notebook with the visible ip address (to use the notebook from outside a VM).
